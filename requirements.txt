# Compatibility install for tools that expect a repo-root requirements file.
# This installs the local package and resolves dependencies from pyproject.toml,
# including GitHub-hosted dependencies that require `git` to be available.
.